<h2>About Us</h2>

<p>This website provide you with all the hotels of your choiced location and price. The ratings and reviews of the guests helps you to find the best hotel to spend you holidays and stay. You can search hotels based on Name, Location, Minimum price, Maximum price and check-in and check-out dates.</p>
<p><%= d=Booking.first.check_out.to_i %></p>
<p><%= b=Booking.first.check_in.to_i %></p>
<p><%= d-b %></p>
<p><%= (Search.last.check_out-Search.last.check_in).to_i %></p>
<%= if Search.last.check_in.present?
      if Search.last.check_out.present?
        available=Array.new
        stay = (Search.last.check_out - Search.last.check_in).to_i
        hot=Hotel.all
          hot.each do |hotel|
            books=hotel.bookings.order(check_in: :asc)
            d=DateTime.now
            d=d.to_i
                books.each do |book|
                    if ((book.check_in.to_i-d) >= stay)
                      available<<book.hotel
                    end
                  d=book.check_out.to_i
                end
          end
    available
      end

end %>
